@using Microsoft.AspNetCore.Identity
@using PortalWebApp.Areas.Identity.Data

@inject SignInManager<PortalWebAppUser> SignInManager
@inject UserManager<PortalWebAppUser> UserManager

<ul class="navbar-nav">
@if(TempData["LoginCheck"] != null)
{
    <li class="nav-item">
        <div id="manage" class="nav-link text-dark"  title="Manage"> Hello
            @{
                if (TempData["Username"] != null)
                {
                     @TempData["Username"]
                }
                TempData.Keep("Username");
                TempData.Keep("LoginCheck");
            }

        </div>
    </li>
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>
}
@*else
{
   
    <li class="nav-item">
        <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}*@
</ul>
